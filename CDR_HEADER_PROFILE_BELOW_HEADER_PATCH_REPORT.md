# CDR Header Profile Below Header Patch

Applied layout update:

- Moved the Live workbook profile / Data Source information card directly below the main header.
- Kept tabs and action buttons in one row below the profile card.
- Kept filters aligned under the tabs/actions row.
- Preserved all existing KPI cards, data, charts, tables, filters, parsing logic, fleetmap mapping, exports, and reports.
- Removed duplicate profile display from inside the Global View content so the profile appears once only.

Build note:
- Full build was not executed in the container because dependencies are not installed.
