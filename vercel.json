{
  "framework": "vite",
  "installCommand": "yarn install --frozen-lockfile --network-timeout 600000 --registry https://registry.npmjs.org",
  "buildCommand": "yarn build",
  "outputDirectory": "dist",
  "functions": {
    "api/**/*.js": {
      "runtime": "nodejs22.x"
    }
  }
}
